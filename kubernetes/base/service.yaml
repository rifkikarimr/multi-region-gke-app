apiVersion: v1
kind: Service
metadata:
  name: multi-region-gke-app
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: multi-region-gke-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
